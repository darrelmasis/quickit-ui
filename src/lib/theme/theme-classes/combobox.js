import { FORM_FIELD_THEME_CLASSES } from "./form-field";

export const COMBOBOX_THEME_CLASSES = {
  light: {
    invalid: FORM_FIELD_THEME_CLASSES.light.invalid,
  },
  dark: {
    invalid: FORM_FIELD_THEME_CLASSES.dark.invalid,
  },
};
