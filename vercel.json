{
  "framework": "vite",
  "buildCommand": "npm run build:docs",
  "outputDirectory": "dist-docs",
  "rewrites": [
    {
      "source": "/((?!.*\\.[^/]+$).*)",
      "destination": "/index.html"
    }
  ]
}
